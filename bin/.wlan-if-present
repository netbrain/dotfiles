#!/bin/bash
# Initiates wlan through wpa_supplicant if a Wireless device is present

if lspci | grep Wireless >& /dev/null; then
	echo "Wireless detected"
	sudo /sbin/wpa_supplicant -i wlan0 -c $HOME/.config/wpa_supplicant.conf -B
	sudo dhclient -nw wlan0
fi
