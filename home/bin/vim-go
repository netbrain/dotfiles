#!/bin/bash

HFD="$PWD/${@:1,-1}"
CFD="/root$HFD"
FLAGS="${@:1:$#-1}"

echo "docker run -it \
	--rm \
	--name vim-go \
	-e GOPATH=/home/docker/dev/go:/home/docker/dev/host \
	-v $(dirname $HFD):$(dirname $CFD) \
	-v $GOPATH:/home/docker/dev/host
	netbrain/vim-go $FLAGS $CFD"
